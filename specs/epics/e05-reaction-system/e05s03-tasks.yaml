# Story: AI chooses optimal reaction by composable priority
story_id: e05s03
title: "AI chooses optimal reaction by composable priority"
status: todo
bcps: 3

tasks:
  - id: e05s03t01
    description: "reaction_value_of_instructions scores harmful incoming effects"
    verify: 'cd backend && python -c "from src.abilities import reaction_value_of_instructions; print(''reaction value'')"'
    status: todo

  - id: e05s03t02
    description: "point_is_in_aiming_result checks if position is in danger zone"
    verify: 'cd backend && python -c "from src.abilities import point_is_in_aiming_result; print(''point in aiming'')"'
    status: todo

  - id: e05s03t03
    description: "reaction_resource_conservation penalizes using scarce resources"
    verify: 'cd backend && python -c "from src.abilities import reaction_resource_conservation; print(''resource conservation'')"'
    status: todo

  - id: e05s03t04
    description: "AI picks the highest-scored reaction choice"
    verify: "cd backend && python -m pytest tests/test_reactions.py -x -q -k 'priority' 2>&1 | tail -10 || echo 'No priority test yet'"
    status: todo
