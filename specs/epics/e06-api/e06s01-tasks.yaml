# Story: GET /heroes returns available hero class names
story_id: e06s01
title: "GET /heroes returns available hero class names"
status: todo
bcps: 1

tasks:
  - id: e06s01t01
    description: "GET /heroes returns 200 with list of strings"
    verify: "cd backend && python -m pytest tests/test_api.py -x -q -k 'heroes' 2>&1 | tail -5"
    status: todo

  - id: e06s01t02
    description: "Response includes all registered hero names"
    verify: 'cd backend && python -c "from src.hero_registry import list_heroes; heroes=list_heroes(); assert ''Tracer'' in heroes or ''tracer'' in ''''.join(heroes).lower()"'
    status: todo
