# Story: Grid places entities and validates positions
story_id: e01s01
title: "Grid places entities and validates positions"
status: passing
bcps: 1

tasks:
  - id: e01s01t01
    description: "Write unit test for Grid initialization and is_valid"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_grid 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Unit"
        - "Core Engine"

  - id: e01s01t02
    description: "Write unit test for get_range with diagonal-first semantics"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_get_range 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Unit"
        - "Core Engine"

  - id: e01s01t03
    description: "Write unit test for get_path (open/blocked/full-block/start==target)"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_path 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Unit"
        - "Core Engine"

  - id: e01s01t04
    description: "Write unit test for push/pull movement along grid"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_push_pull 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Unit"
        - "Core Engine"

  - id: e01s01t05
    description: "Write unit test for line-of-sight (clear, blocked, cover)"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_line_of_sight 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Unit"
        - "Core Engine"

  - id: e01s01t06
    description: "Write integration test for get_points_in_range w/ LOS blocking"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_blocked_by_los 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Integration"
        - "Core Engine"

  - id: e01s01t07
    description: "Write integration test for get_movable_spaces w/ enemy blocking"
    verify: "cd backend && python -m pytest tests/test_grid.py -x -q -k test_movable 2>&1 | tail -5"
    risk: P2
    status: skipped
    note: "Needs full Engine + Entity setup — deferred to integration story"
    allure:
      severity: normal
      categories:
        - "Integration"
        - "Core Engine"

  - id: e01s01t08
    description: "Run full test suite to confirm no regressions"
    verify: "cd backend && python -m pytest tests/ -x -q 2>&1 | tail -5"
    risk: P2
    status: passing
    allure:
      severity: normal
      categories:
        - "Regression"
        - "Core Engine"
