Actual action space is very high and has compound actions. Reduce into list of PlausibleActions eg Move 3 toward and use Slash on nearest enemy.

Use ML to score each PlausibleAction.

Gamestate->Transformer->Encoded Context Vector
Context + Encode self + encode destination + embed ability + sum encode targets (+ possibly other values like what terrain is moved through, new distances to nearby units, etc)
No MCTS.

Train a network to predict gamestate value based on the true eventual winner.
Train the action evaluator based on estimated change in value.
